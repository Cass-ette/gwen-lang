module ledger_queries
use print_lines from ledger_view

export func print_query_results(ledger: Ledger, title: string, category: string = "", kind: string = "", month: string = "", note_keyword: string = "")
  lines := ledger.list_entries(category, kind, month, note_keyword)
  print_lines(title, lines)
endfunc
endmodule
